import 'package:flutter/material.dart';

class AppConstants {
  const AppConstants._();

  static const appName = 'Offline Translator';
  static const hiveBoxName = 'translator_app';
  static const splashDuration = Duration(seconds: 1);
  static const defaultSourceLanguageCode = 'en';
  static const defaultTargetLanguageCode = 'tr';
  static const pagePadding = EdgeInsets.all(20);
}
